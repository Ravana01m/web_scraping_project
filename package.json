{
  "name": "review-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack web scraping & review intelligence platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd server && npm start",
    "dev": "cd frontend && npm run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "uuid": "^10.0.0"
  }
}
